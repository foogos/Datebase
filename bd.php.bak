<?php
require_once'Connect.php';
?>
<form action="book_author.php" method="GET">
 Регион:<select name="id_region">
 <?php
$result=mysqli_query($link,"SELECT
  region.region
FROM region");
$rows=mysqli_fetch_all($result,MYSQLI_ASSOC);
foreach ($rows as $row)
{
	echo"<option>". ($row['region']."</option>");
}
?>
 </select><br>
<input type="submit" name="submit" value="Поиск"><br>
</form>
<?php
if($_GET['submit'])
{
	$result=mysqli_query($link,"SELECT
  *
FROM country
  INNER JOIN region
    ON country.id_region = region.id_region
WHERE region.id_region ='$_GET[id_region]'");
$rows=mysqli_fetch_all($result,MYSQLI_ASSOC);
foreach ($rows as $row)
{
	print ($row['region']."<br>");
}
}
?>